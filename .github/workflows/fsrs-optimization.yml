name: FSRS Optimization

on:
  schedule:
    - cron: "0 10 1 * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  optimize:
    name: Trigger FSRS Optimization
    if: github.repository != 'joaogiacometti/notorium'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Trigger scheduled FSRS optimization
        env:
          NOTORIUM_APP_URL: ${{ secrets.NOTORIUM_APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          if [ -z "$NOTORIUM_APP_URL" ] || [ -z "$CRON_SECRET" ]; then
            echo "Skipping FSRS optimization because required secrets are not configured."
            exit 0
          fi

          curl --fail-with-body --silent --show-error \
            --max-time 300 \
            -X GET \
            -H "Authorization: Bearer $CRON_SECRET" \
            -H "x-vercel-protection-bypass: $VERCEL_AUTOMATION_BYPASS_SECRET" \
            "${NOTORIUM_APP_URL%/}/api/flashcards/fsrs/optimize"
